# ──────────────────────────────────────────────────────────────────────────────
# This file is no longer used.
#
# WTLL Platform has moved to:
#   Backend  →  Railway  (backend/railway.toml)
#   Frontend →  Vercel   (frontend/vercel.json)
#   Database →  Neon PostgreSQL (connection string set as DATABASE_URL in Railway)
#
# See the README or the individual config files above for setup instructions.
# ──────────────────────────────────────────────────────────────────────────────
