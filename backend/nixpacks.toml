# Nixpacks configuration for Railway deployment
# stdenv.cc.cc.lib provides libstdc++.so.6 which pandas/numpy C extensions require

[phases.setup]
nixPkgs = ["python3", "postgresql_16.dev", "gcc", "stdenv.cc.cc.lib"]
